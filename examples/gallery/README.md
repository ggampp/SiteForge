# SiteForge examples gallery

Next.js UI that embeds static clones from `examples/clones`.

```bash
# from repo root
node examples/scripts/sync-clones-to-gallery.mjs
cd examples/gallery
pnpm install
pnpm dev
```

Open http://localhost:4310
